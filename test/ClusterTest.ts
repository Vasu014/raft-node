describe('Running leader election tests', () => {
    it('should start every server in the cluster', () => {

    })

    it('should establish connections between all the nodes in the cluster', () => {

    })

    it('should have every server in FOLLOWER state initially', () => {

    })

});